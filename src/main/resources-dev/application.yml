spring:
  profiles:
    active: dev
    include: common

---
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/recap
    username: root
    password: qwer1234