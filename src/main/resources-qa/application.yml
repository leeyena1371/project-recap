spring:
  profiles:
    active: qa
    include: common

---
spring:
  config:
    activate:
      on-profile: qa

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/recap
    username: root
    password: qwer1234